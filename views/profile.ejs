<% include partials/page-top %>


  <div class="row">

    <% if(user.image) { %>
      <div class="col-md-2 col-lg-1 mt-5 mt-md-2 text-center text-md-left"><img src="<%= user.image %>" alt="<%= user.username %>"></div>
    <% } %>

    <div class="col-md-10 col-lg-11 mt-1 mt-md-2 text-center text-md-left"><h2><%= user.username %></h2></div>

  </div>


  <div class="row justify-content-center text-center mt-4" id="gameBoard">
    <div class="col">

      <% if(typeof game !== 'undefined') { %>

        <% if(game.matches.length - game.current_match == 0) { %>

          <h2>Congratulations!</h2>

          <p>Here is your top 100 American films of all time!</p>

          <ol class="sorted-list">

            <% for (var i = 0; i < game.movies.length; i++) { %>
                <li>
                  <div class="movie">
                    <img src="<%= game.movies[i].Poster %>" alt="<%= game.movies[i].Title %>"><h2><%=  game.movies[i].Title %></h2><p><%= game.movies[i].Year %></p>
                  </div>
                </li>
            <% } %>

          </ol>

          <ul class="list-unstyled">
            <li class="mt-4">
              <button type="button" class="btn btn-lg btn-primary delete">Delete Game <i class="fas fa-trash-alt"></i></button>
            </li>
            <li class="mt-3">
              <button type="button" class="btn btn-lg btn-secondary copy" data-clipboard-target="#gameBoard ol">Copy List <i class="fas fa-copy"></i></button>
            </li>
          </ul>

          <div id="user" class="<%= user.id %>" style="display:none"></div>

        <% } else { %>

          <p>You're doing amazing. Only <%= game.matches.length - game.current_match %> matches left. Keep up the great work. You're almost there.</p>

          <ul class="list-unstyled justify-content-center text-center">
      			<li class="mt-5"><a href="/game" class="btn btn-xl btn-primary">Continue <i class="fas fa-play-circle"></i></a></li>
      		</ul>

        <% } %>

      <% } else { %>

        <p>Ready to take your game to the next level? Enter the American Film Institute's Top 100 American Films of all time tournament. This is no small task. It will require <strong>4,950 battles</strong>. Don't worry you can save your game at any time and come back and pick it up right where you left off. Only got a couple of minutes? That's enough for up to 50 battles.</p>

  		  <h2 class="mb-4 mx-2 text-center">Let the blood bath begin!</h2>

    		<ul class="list-unstyled justify-content-center text-center">
    			<li class="mt-5"><a href="/game" class="btn btn-xl btn-primary">FIGHT <i class="fas fa-play-circle"></i></a></li>
    			<li class="mt-4"><button type="button" class="btn btn-secondary" data-toggle="modal" data-target="#instructionsModal">Instructions <i class="fas fa-info-circle"></i></button></li>
    		</ul>

      <% } %>

    </div>
	</div>

  <% include partials/instructions %>


<% include partials/page-bottom %>
